import { LivreExemplaireModel } from './livre-exemplaire-model';

describe('LivreExemplaireModel', () => {
  it('should create an instance', () => {
    expect(new LivreExemplaireModel()).toBeTruthy();
  });
});
