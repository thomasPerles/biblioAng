import { NotificationUtilisateur } from './notification-utilisateur';

describe('NotificationUtilisateur', () => {
  it('should create an instance', () => {
    expect(new NotificationUtilisateur()).toBeTruthy();
  });
});
